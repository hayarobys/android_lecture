<?xml version="1.0" encoding="utf-8"?>
<!--레이아웃 파일의 이름엔 소문자,숫자,언더바만 사용 가능하다-->
<LinearLayout
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="wrap_content"
	android:layout_height="wrap_content"
	android:orientation="horizontal"
	android:background="#9f9797">

	<ImageView
		android:layout_width="200dp"
		android:layout_height="400dp"
		android:background="#0fafff"
		android:src="@drawable/tulips"
		android:scaleType="center"
		android:padding="20dp"
		android:cropToPadding="true"/>
	<!--android:cropToPadding="true" - 기본값은 false. padding 속성과 함께 사용.
		scaleType이 center일 경우 이미지뷰엔 padding이 적용되지 않지만,
		true 일 경우 padding값을 적용한다.

		fitXY는 cropToPadding이 있던 말든 padding에 영향을 받음.

		※ padding이 View에서 제공되는 공통 기능임에도 ImageView에서 제대로 적용이 안되는 이유는
			ImageView가 cropToPadding과 함께 작동하도록 오버라이딩을 해놓았기 때문이다.
	-->

</LinearLayout>
